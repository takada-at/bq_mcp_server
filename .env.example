# .env.example - BigQuery Metadata API Server Configuration

# --- GCP Settings ---
# Optional: GCPサービスアカウントキーのパス（未設定の場合はADCを使用）
GCP_SERVICE_ACCOUNT_KEY_PATH=

# Required: GCPプロジェクトIDのリスト
PROJECT_IDS=

# --- Cache Settings ---
# Optional: キャッシュの有効期限（秒） Defaults to 3600.
CACHE_TTL_SECONDS=3600

# Optional: キャッシュファイルの保存先ディレクトリ Defaults to .bq_metadata_cache.
CACHE_FILE_BASE_DIR=.bq_metadata_cache

# --- API Server Settings ---
# Optional: APIサーバーのホスト名 Defaults to 127.0.0.1.
API_HOST=127.0.0.1

# Optional: APIサーバーのポート番号 Defaults to 8000.
API_PORT=8000

# --- Query Execution Settings ---
# Optional: クエリの最大スキャンバイト数 Defaults to 1073741824.
MAX_SCAN_BYTES=1073741824

# Optional: デフォルトのクエリ結果制限数 Defaults to 100.
DEFAULT_QUERY_LIMIT=100

# Optional: クエリのタイムアウト秒数 Defaults to 300.
QUERY_TIMEOUT_SECONDS=300
