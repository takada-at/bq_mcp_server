# .env.example - BigQuery Metadata API Server Configuration

# --- GCP Settings ---
# Optional: GCP service account key path (uses ADC if not set)
GCP_SERVICE_ACCOUNT_KEY_PATH=

# Required: List of GCP project IDs
PROJECT_IDS=

# Optional: List of dataset filters (e.g., pj1.*,pj2.dataset1)
DATASET_FILTERS=

# --- Cache Settings ---
# Optional: Cache TTL in seconds Defaults to 3600.
CACHE_TTL_SECONDS=3600

# Optional: Cache file storage directory Defaults to .bq_metadata_cache.
CACHE_FILE_BASE_DIR=.bq_metadata_cache

# --- API Server Settings ---
# Optional: API server hostname Defaults to 127.0.0.1.
API_HOST=127.0.0.1

# Optional: API server port number Defaults to 8000.
API_PORT=8000

# --- Query Execution Settings ---
# Optional: Maximum scan bytes for queries Defaults to 1073741824.
MAX_SCAN_BYTES=1073741824

# Optional: Default query result limit Defaults to 100.
DEFAULT_QUERY_LIMIT=100

# Optional: Query timeout in seconds Defaults to 300.
QUERY_TIMEOUT_SECONDS=300
